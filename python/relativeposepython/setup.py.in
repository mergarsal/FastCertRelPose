from setuptools import setup

setup(
    name='relativeposepython',
    version='1.0.0',
    author='Mercedes Garcia Salguero',
    author_email='mercedesgarsal@uma.es',
    description='Python binding for the Relative Pose Problem with central cameras',
    package_dir={'': '${CMAKE_CURRENT_BINARY_DIR}'},
    packages=['relativeposepython'],
    package_data={'': ['*.so']}
)
